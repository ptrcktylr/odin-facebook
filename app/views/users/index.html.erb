<ul>
  <% @users.each do |user| %>
    <li><%= user.name %></li>
  <% end %>
</ul>

<%= session.to_hash %>
-------------------
<%= current_user %>

<%= link_to("Sign Out", destroy_user_session_path, :method => :delete) %>